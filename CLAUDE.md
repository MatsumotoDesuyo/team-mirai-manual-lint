# CLAUDE.md

Claude Code 用のプロジェクト指示書。本リポジトリで作業する際は、まず本ファイルと [DESIGN.md](./DESIGN.md) を必ず先に読むこと。

## このプロジェクトについて

`team-mirai-manual-lint` は、Google Docs 上で執筆されるチームみらい街頭活動マニュアルが、チームみらいの「マニュアル作成ガイドライン」に適合しているかを機械的にチェックするツール群。**個人サポーターによる非公式のコミュニティツール**であり、チームみらい公式のものではない。

設計判断・アーキテクチャ・採用/却下案の根拠はすべて [DESIGN.md](./DESIGN.md) に集約されている。**新しい設計判断や既存判断への異論を述べる前に、必ず DESIGN.md を読むこと。**

## ユーザーへの呼称

ユーザーは「ミッションオーナー」と呼ぶ。固有名は使わない。

## 振る舞い指針

- **迎合しない**。前提の穴・論理の弱点・抜けは率直に指摘する。直接的な批評を歓迎する人物。
- **書式チェックは決定論層（Layer A / GAS）の責務**。色・pt・余白・コントラスト比・装飾比率など `documents.get` の値で確定判定できる項目に LLM を噛ませない。
- **意味判断のみ Layer B（LLM）の責務**。一文長・受動態・二重否定・曖昧副詞・トーン・「強調を文章で言い換えるべきだったか」等。
- **法令・著作権・肖像権は人間専管**。機械チェックの硬ゲートにしない。機械結果に関わらず人間レビュー必須。
- **個人サポーター名義公開のみ**。チームみらい組織名義での公開・配布権限なし。READMEや公開文面で「個人による非公式コミュニティツール」を明示する。

## 却下済みの案（再提案しないこと）

詳細は DESIGN.md「却下案と理由」。**合理的な新材料なしに以下を再提案しないこと**:

1. 書式の決定論チェックを LLM に担わせる案 — 確定値判定に推測誤差を持ち込むため
2. 意味チェックを Gemini in Google Docs で代替する案 — 自動化・再現不可、各個人有料契約必要
3. Google Workspace Marketplace で Add-on 公開する案 — 機微スコープOAuth検証要、個人名義×未検証で100ユーザーキャップ＋警告画面、信頼性問題

## 自動化しない領域（人間確認に委ねる）

- 総ページ数・改ページ後のレイアウト（`documents.get` で取得不可）
- 法令の正誤
- 画像の出所・許諾、通行人の写り込み判断

## ガイドラインの参照と更新

判定基準であるチームみらい「マニュアル作成ガイドライン」のローカルキャッシュは [GUIDELINES.md](./GUIDELINES.md)。原本は Google Docs（ファイル ID: `1ht9fGSHIf0zjiV7INp7JvnCCZHwoJYMCdOB-VkokO1c`）。

- 判定ルールを参照する際は **必ず GUIDELINES.md を読むこと**。原本を毎回 API で取りに行かない。
- ミッションオーナーから「ガイドラインを更新して」等の依頼があった場合のみ、Google Drive MCP の `read_file_content` でファイル ID `1ht9fGSHIf0zjiV7INp7JvnCCZHwoJYMCdOB-VkokO1c` を再取得し、GUIDELINES.md を上書きする。フロントマターの `modified_time`（Drive 上の最終更新）と `fetched_at`（ローカル取得日）も更新すること。
- 上書き前に `git diff` で差分を確認し、コミットメッセージに「ガイドライン更新（Drive: YYYY-MM-DD 更新分）」のように Drive 側の更新日を残す。

## 出力スタイル

- 回答は日本語。
- コミットメッセージは日本語可。「何を変えたか・なぜを簡潔に」。
- コード内コメントは原則書かない。書く場合は「なぜ」が非自明な箇所に限定し、短く。