# DESIGN — team-mirai-manual-lint

本ドキュメントは設計判断の意思決定ログ。**新規判断・既存判断への異論はまず本ドキュメントを読んでから**行うこと。

---

## 1. 目的

Google Docs 上で執筆されるチームみらい街頭活動マニュアルが、チームみらいの「マニュアル作成ガイドライン」に適合しているかを機械的にチェックする。

最終的な校正は校正チーム（人間）が担う。本ツールはその前段フィルタとして、機械で確実に潰せる項目を潰し、人間レビューを判断項目（特に法令・権利・トーン）に集中させることを目的とする。

## 2. 制約（設計の前提）

これらは外部条件であり、設計の自由度を規定する。

1. **作成者は全国の多数サポーター**。大半が非技術者、大半が消費者 Gmail アカウント。
2. **公開はいち個人サポーター名義**。チームみらい組織名義での公開・配布権限なし。
3. **Google Docs スコープは OAuth「機微スコープ」**。公開アプリは検証審査必須、未検証は新規100ユーザーキャップ＋「未確認アプリ」警告画面。
4. **Gemini in Google Docs は Layer B にならない**:
   - 消費者 Gmail で素では使えず、Workspace Gemini 対応プラン or Google AI / One AI Premium 等が必要
   - 対話型 UI 機能で自動化・再現・構造化レポート出力ができない
5. **最終ゲートは人間（校正チーム）**。機械は前段フィルタに徹する。
6. **ガイドラインは進化する**。ガイドライン自身が「知見を集約してアップデートしていきましょう」と明言。**ルールのドリフトを起こさない配布設計**が必須。

## 3. アーキテクチャ — 決定性で割った3層

ツール種別（GAS / LLM）ではなく、**判定の決定性**で層を割る。

### Layer A — 決定論チェック（全員必須・GAS）

ガイドラインの強制可能項目の概ね8割を網羅。GAS の `DocumentApp` / Advanced Docs Service（`documents.get` 相当）から得られる構造化データに対し、確定的な数式・完全一致・カウントで判定する。

対象例:

- 文字色 #000000、禁色 #64d8c6（ミントグリーン）の不使用
- WCAG コントラスト比（固定式：相対輝度→比率）
- フォントファミリ（Noto Sans JP / Noto Sans、`namedStyles` 継承解決込み）
- フォントサイズ階層（タイトル21pt／見出し1: 18pt／見出し2: 15pt／見出し3: 13pt／本文11pt）と 11pt 未満禁止（引用例外あり）
- 行間 1.15、余白 2cm（上下左右）、段落初め字下げ 0、左揃え
- 表セル（ヘッダ #666666 背景／#ffffff 罫線 1pt 等）
- 強調比率（装飾文字数 ÷ 総文字数 ≦ 1割）
- 絵文字・機種依存文字検出（Unicode 範囲）
- 作成日・更新日の本文記載
- リンク抽出（URL 形式・到達性は別途）

**LLM は使わない**。確定値判定に推測誤差を持ち込むため。

### Layer B — 意味チェック（少数の指定担当が実行・LLM）

文脈・意味理解が必要な項目。公開リポジトリの CLI / GitHub Action として実装し、LLM API を使える少数の指定担当者が校正チーム前段で実行する。

対象例（ガイドライン第3章「わかりやすさ」中心）:

- 一文1トピック、一文長（推奨50字／最大100字）、読点3つ以下、漢字連続6字以下
- 受動態回避、二重否定禁止、冗長表現（「〜することができる」等）
- 曖昧副詞（適宜、適切に、しばらく、丁寧に 等）
- トーン／「チームみらいらしさ」適合
- 「強調は文章で言い換えるべきだったか」（Layer A が検出した強調箇所への意味判断）
- 用語集（チームみらい運用の「校正用ルール・用語集」スプレッドシート）との整合
- 「読み手視点で書けているか」「初心者にわかるか」の助言

### Layer C — 人間（校正チーム）

機械レポートを入力に、判断項目へ集中する。

- **法令・著作権・肖像権は機械結果に関わらず必須目視**（ガイドライン自身が法令は人間確認必須と明記）
- 機械レポートは硬ゲートにしない。`ERROR` ゼロでも人間レビューは必ず通す

## 4. 配布方式

組織名義不可・全国消費者 Gmail・Add-on 検証壁を踏まえた現実解。

### Layer A の配布：テンプレート Doc コピー + リモートローダ

- **配布物は「スクリプトを含むテンプレート Google Doc」**。利用者は **Doc を「コピーを作成」するだけ**で導入完了。
- バウンドスクリプトは **薄いローダ** にし、実行時に **GitHub raw URL から最新のチェッカーコード ＋ `rules.json` を取得して実行**する（`UrlFetchApp`）。
- 利点:
  - Marketplace 不使用 → OAuth 検証不要・100ユーザーキャップなし・「未確認アプリ」警告なし・組織名義不要
  - コピー後もロジック／ルール更新が中央から伝播 → **ドリフト断**
  - 消費者 Gmail で動作、非技術者は「コピー1クリック」で導入可能
- 留意:
  - 外部コード取得（`UrlFetchApp`）の透明性は導入ガイドで明示し、公開リポジトリのコード監査可能性で正当化する
  - スコープ自体は非機微（Doc 本体への読み書きは bound script として owner 権限で行う）

### Layer B の配布：公開リポジトリの CLI / GitHub Action

- 公開リポジトリ内で完結する CLI。LLM 担当者が手元で実行、または Action で起動。
- 各サポーター個人の有料 LLM 契約に依存させない。担当者の API キーで集中運用。

### ルール・用語集の管理（正本一元化）

- **正本は単一**:
  - 本リポジトリの `rules.json`（バージョン管理）
  - ガイドライン運用チームが管理する「校正用ルール・用語集」スプレッドシート
- Layer A / B は実行時に正本を取得。**コードにハードコードしない**。
- 公開リポジトリにはコード・rules.json・導入ガイドのみ。**秘密情報（LLM API キー等）は置かない**。

## 5. 却下案と理由

将来の蒸し返し防止のため、却下した案とその理由を明示する。

### 却下案1: 書式チェックを LLM に担わせる

提案時の構造: 「`documents.get` で JSON 取得 → LLM に書式ルール判定させる」。

却下理由:

- 色・pt・余白・コントラスト比は **WCAG 等の固定式／完全一致** で答えが一意。LLM を噛ませると、無料・厳密・即時で出る判定に課金・遅延・非決定性を持ち込み精度を下げる。
- 特に配色節（コントラスト比、禁色検出、強調比率）は **スクリプトが最も得意・LLM が最も苦手** な領域。LLM は色を「見る」ことができず、結局 RGB 数値を受け取って固定式を不正確に当てるだけ。

### 却下案2: Layer B を Gemini in Google Docs で代替

却下理由（致命的に2つ）:

1. **無料ではない**: 消費者 Gmail で素では使えず、Workspace Gemini 対応プラン or Google AI / One AI Premium 等が必要。「API キー1本 → 全サポーターが各自有料契約」となり、ボトルネックが拡大する。
2. **自動化・再現不可**（決定的）: UI 上の対話型コパイロットで、固定ルールセットを当てて構造化レポートを出すことができない。実行ごとに揺れ、ルール網羅も保証されず、Layer C に渡せる機械可読結果も出ない。**パイプライン段にはなり得ない**。

任意の作成者個人補助としての利用は妨げないが、Layer B の代替にも前段ゲートにもしない。

### 却下案3: Google Workspace Marketplace で Add-on 公開

却下理由:

- Docs スコープは「機微スコープ」→ 公開には **OAuth 検証審査必須**（デモ動画提出・プライバシーポリシー・ブランド確認、承認まで数週〜数ヶ月）
- 未検証のまま公開すると **新規100ユーザーキャップ ＋ 「Google 未確認アプリ」警告画面**。全国規模で破綻
- **組織名義公開権限なし**。個人名義で機微スコープ警告画面が出る構図は政党マニュアル運用の信頼性と齟齬
- ローカルアップロード方式は存在しない（Add-on は Apps Script プロジェクトからデプロイ）

採用した「テンプレート Doc コピー + リモートローダ」がこの全てを回避する。

## 6. 自動化しない領域（人間確認に委ねる）

- **総ページ数・改ページ後のレイアウト**: `documents.get` は自動改行後のレンダリングページ数を返さない。「前編2ページに収まる」「フッター 1/5 の総ページ数妥当性」は機械判定不可。
  - 将来検討: Drive API で PDF エクスポート → ページ数カウント
- **法令の正誤**: ガイドライン自身が「法令は必ず目視確認」と明記
- **画像の出所・許諾・通行人の写り込み**: API から判定不可。画像存在検出と近傍の出典テキスト有無の確認に留まる

機械レポートには **「人間確認 / 自動化対象外」** を明示タグで出す。「自動化したフリ」をしない。

## 7. レポート形式（方針）

- 構造化（ルールID・該当箇所インデックス・重大度・自動修正可否・修正提案文）
- **重大度を分離**:
  - `ERROR` = Layer A 決定論違反（確実）
  - `WARN`  = Layer B LLM 助言（参考）
  - `INFO`  = 人間確認領域の提示
- 校正チーム（Layer C）が「何が確実で何が参考か」を即判別できることが目的

## 8. 次の成果物

**判定マトリクス（`rules.json` の設計図）** をまず作る。ガイドライン全項目を以下に仕分けたもの:

| 列 | 内容 |
|---|---|
| ルール ID | 安定識別子 |
| ガイドライン参照 | 章節 |
| Layer 割り当て | A / B / C / 自動化不可 |
| 重大度 | ERROR / WARN / INFO |
| 判定方法 | Apps Script API メソッド or LLM プロンプト戦略 |
| 参照する正本 | rules.json 内項目 / 用語集スプレッドシート |
| 想定誤検出パターン | 既知の難所 |

これを先に作り、実装はマトリクスを満たす形で進める。

## 9. 直近の意思決定履歴（要点のみ）

- `team-mirai-manual-lint` という命名で個人サポーター公開とする。README で「非公式・コミュニティツール」を明示する。
- 既存の親プロジェクト（trinity-nexus）とは独立した別リポジトリとして開始する。設計コンテキストは本ドキュメントと CLAUDE.md で持ち運ぶ。